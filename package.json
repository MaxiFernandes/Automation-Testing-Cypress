{
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "regression": "cypress run --spec 'cypress/e2e/regression/**/*.cy.js'",
    "known-bugs": "cypress run --spec 'cypress/e2e/known-bugs/**/*.cy.js'",
    "chrome": "cypress run --browser chrome",
    "headed": "cypress run --headed",
    "ci": "cypress run --spec 'cypress/e2e/regression/**/*.cy.js'"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.0.1",
    "cypress": "^13.13.1",
    "cypress-mochawesome-reporter": "^4.0.2",
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^5.1.1",
    "mochawesome-report-generator": "^6.3.2"
  }
}
